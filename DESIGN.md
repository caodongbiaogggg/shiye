# 拾页 · 学习手记：设计与维护基线

状态：用户已认可第一版视觉方向，并要求保存设计风格和思路，供后续修改使用。
记录日期：2026-09-08。本文记录当前实现；后续如用户调整方向，同步更新此文档。

## 网站定位

个人学习总结与日常博客的长期存放处。读者首先能找到文章、舒适阅读，再了解作者。内容分为学习笔记、实践记录、日常随笔。

设计关键词：安静、清晰、有笔记感、留白充足、适合中文阅读。
暂用站名「拾页 · 学习手记」。站名、个人介绍与四篇文章仍是示例，认可设计不代表它们已经成为正式内容。

## 视觉思路

以“翻开一本持续书写的个人笔记本”为主题。首页大标题讲清记录的目的，右上角略微倾斜的页边手记是主要识别元素；蓝色下划线像阅读时的标记。其他区域保持规整，让视觉焦点最终回到文章。

保留当前轻盈的蓝灰配色、中文宋体标题、克制的线条图标。后续新增模块优先沿用已有间距、颜色与卡片规则。全站重做配色、大面积插画、轮播和强动效不属于当前方向；如确需改变，以用户新要求为准。

## 色彩基线

| 用途 | CSS 变量 / 色值 | 使用范围 |
| --- | --- | --- |
| 页面纸面 | `--paper: #F5F7FA` | 全站背景 |
| 墨蓝 | `--ink: #233B57` | 主标题、正文、选中分类底色 |
| 强调蓝 | `--blue: #345CE5` | 品牌、链接、分类与焦点 |
| 辅助文字 | `--muted: #647386` | 摘要、日期、次要说明 |
| 分隔线 | `--line: #DCE3EC` | 边框、列表分隔 |
| 浅蓝 | `--wash: #EAF0FF` | 轻量悬停反馈 |
| 白色 | `--white: #FFFFFF` | 精选文章卡片 |
| 介绍卡背景 | `#EAF0F8` | 右栏介绍 |
| 标题下划线 | `#DCE5FF` | 首页标题强调 |

## 字体与排版

- 首页和文章主标题：STSong / SimSun / serif；宋体营造中文手记气质。
- 正文与导航：Segoe UI / Microsoft YaHei / sans-serif，保障屏幕阅读清晰度。
- 页边手记：KaiTi / STKaiti / SimSun / serif。
- 日期与计数：Consolas / monospace。
- 使用系统字体，无外部字体下载；不同设备缺少对应字体时外观会略有差异。
- 首页主标题桌面约 40–58px，文章主标题 40px，手机 31px；正文 17px、行高 2.1，摘要 16px、行高 1.9。
- 常用操作文字 14px；日期、标签等次要信息 12–13px。后续增加内容时避免为了塞进布局而缩小字号。

## 页面结构与尺寸

桌面首页结构：

```text
品牌 / 学习手记                         文章 · 关于
--------------------------------------------------
网站主题与文章入口                  页边手记

最近写下的                          个人介绍
全部 / 学习 / 实践 / 随笔            笔记分册
精选文章                            简短寄语
文章列表
--------------------------------------------------
页脚
```

- 主内容最大宽度 1160px；桌面文章区与 284px 侧栏并排，间距 62px。
- 首篇精选使用白底、12px 圆角、细边框与左侧蓝色标记。普通文章主要依靠分隔线组织，保持层级差异。
- 正文阅读区最大宽度 730px，保留返回入口、分类、日期、摘要、段落标题。
- 1250px 以下减少外围空间；900px 以下侧栏缩至 235px；700px 以下改为单列，侧栏移到文章之后，隐藏页边装饰；410px 以下侧栏内部也为单列。
- 文章卡片悬停轻移 3px；保留键盘焦点、跳转正文入口和减少动态效果偏好。

## 交互与内容约定

- 分类切换只过滤文章；标签是文章信息，不伪装成可点击入口。
- 文章与关于页使用 hash 地址，如 `#learning-loop`、`#about`；浏览器前进后退通过 hashchange 更新。
- 文章源文件在 `content/posts/*.md`，按日期倒序排列；文件名作为默认 slug，必须唯一，已发布文章保持稳定地址。
- 正文使用 Markdown，支持 GFM 表格、列表、引用、代码、链接和图片；没有网页编辑后台、搜索或登录。
- 独立文章的服务端 SEO 元数据尚未实现；浏览器页面标题会随阅读文章更新。
- 首页分类、导航和正文保持一致的用词；内容由真实写作需求驱动扩展。

## 修改入口

| 想修改什么 | 文件 |
| --- | --- |
| 文章标题、摘要、分类、标签、日期与正文 | `content/posts/*.md` |
| 站名、首页布局、个人介绍、阅读组件 | `app/page.tsx` |
| 配色、字号、间距、断点、动效 | `app/globals.css` |
| 默认网页标题、描述、语言 | `app/layout.tsx` |
| 网站图标 | `public/favicon.svg` |
| 静态导出配置 | `next.config.ts`、`vite.config.ts` |
| GitHub Pages 子路径资源修正与检查 | `scripts/prepare-pages.mjs` |
| GitHub Pages 自动发布 | `.github/workflows/pages.yml` |

## 技术与验证记录

使用当前 React / Vinext 项目结构，静态导出到 `dist/client`。建议 Node.js 22：本机 Node.js 26 曾在构建收尾出现原生退出错误，Node.js 22 构建通过。

GitHub Pages 子路径由构建后的 `prepare-pages.mjs` 处理，读取 BASE_PATH；不要直接恢复 next.config.ts 的 basePath 配置，初版验证时该方式曾导致根 index.html 未导出。

初版已完成：Node.js 22 静态构建、TypeScript 检查、本地 HTTP 200、`/learning-blog` 子目录下 9 个 JS/CSS 资源路径与文件存在性检查。未执行浏览器自动化交互测试。

后续修改检查应与改动相称：文案与文档修改无需额外写测试；改动布局时检查手机及长标题显示；修改路由、构建或发布时检查静态入口及仓库子路径资源。

## 后续交接

2026-09-09 用户确定仓库为 `https://github.com/caodongbiaogggg/shiye`。发布目标为 GitHub Pages 项目站点 `https://caodongbiaogggg.github.io/shiye/`，自定义域名留待后续讨论。部署是否成功以 GitHub Actions 与线上检查为准。

后续可根据用户提供的昵称、简介、真实文章替换示例。Markdown 写作已于 2026-09-09 按用户要求实现。独立文章路由、搜索属于后续方向，尚未列为已授权任务。

维护此文档时补记重要设计变更及原因；普通内容更新无需重写设计基线。

## Markdown 改造记录（2026-09-09）

保留现有视觉与文章 hash 地址。通过 scripts/posts.mjs 读取 YAML 文章信息，校验必填项、有效日期、分类和重复地址，跳过 draft: true 与下划线开头的模板，构建时生成被 Git 忽略的 app/posts.generated.json。Vite 开发时监听 Markdown 增删改。正文由 react-markdown + remark-gfm 渲染，不执行原始 HTML。新增文章默认不是示例；原四篇通过 sample: true 保留示例标记。草稿仅从网站排除，公开仓库内的文件仍公开。
